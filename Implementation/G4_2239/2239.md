2239
==

기본틀
--
```python
ls = []
for i in range(9):
    line = input()
    row = [int(char) for char in line]
    ls.append(row)

def isGood():
    return None

def something(y, x):
    if ls[y][x] == 9:
        ls[y][x] = 0
        return None
    else:
        ls[y][x] += 1
        if isGood():
            return something(next_y, next_x)
        else:
            return something(y, x)
```

```python
ls = []
zero = []
zero_idx = 0

def isGood():
    return None

def something(idx):
    if ls[zero[idx][0]][zero[idx][1]] == 9:
        ls[zero[idx][0]][zero[idx][1]] = 0
        return None
    else:
        ls[zero[idx][0]][zero[idx][1]] += 1
        if isGood():
            if idx + 1 == len(zero):
                return None
            return something(idx + 1)
        else:
            return something(idx)
        
for i in range(9):
    line = input()
    row = [int(char) for char in line]
    ls.append(row)
    
for i in range(9):
    for j in range(9):
        if ls[i][j] == 0:
            zero.append((i, j))
            
something(0)

for cor in zero:
    print(ls[zero[zero_idx][0]][zero[zero_idx][1]])
```